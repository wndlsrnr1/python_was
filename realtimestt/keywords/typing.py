"""
이벤트 루프 기본 개념

단일 스레드 이벤트 루프

하나의 스레드에서 여러 비동기 작업을 순차적으로 처리하는 메커니즘. I/O 대기 시간 동안 달느 작업을 실행하여 효율성을 높인다.

코루틴이 협력적으로 실행

코루틴은 서로 양보하며 실행된다. 하나의 코루틴이 I/O 대기 상태가 되면 자발적으로 제어권을 다른 코루틴에 넘겨준다.

I/O 대기중에만 다른 코루틴이 실행
I/O 작업이 완료되는 동안, 이벤트 루프는 다른 코루틴을 실행합니다. 이를 통해 CPU를 효율적으로 활용합니다. 

이벤트 루프가 블로킹되다.
동기 코드가 이벤트 루프를 차단하는 현상이다. CPU 집약적인 작업이나
동기 I/O 작업이 이벤트 루프를 점유하면 다른 코루틴이 실행되지 않습니다.

asyncio 모듈

asyncio

Python의 비동기 프로그래밍을 위한 표준 라이브러리입니다. 이벤트 루프, 코루틴, 태스크 등을 관리합니다. 

AsyncIterator
비동기 이터레이너 async for 문과 함께 시ㅏ용하여 비동기적으로 데이터를 순회한다.

async for
비동기 이터레이터를 순회하는 문법ㄴ입니다. 각 항목을 가져오는 동안 다른 코루틴이 실핸된다. 

task 객체

task는 코루틴을 실행하고 관리하는 객체, 코루틴을 Task로 래핑하면 이벤트 루프가 스케줄링하고 상태를 추적할 수 있다.

.done()
Task가 완료되었는지 확인하는 메서드입니다. True를 반환하면 Task가 정상 완료되었거나 취소되었거나 예외가 발생.


.cancelled()
Task가 취소되었는지 확인하는 메서드. True를 반환하면 Task가 취소된다.

.exception()
Task에서 발생한 예외를 반환하는 메서드. 정확한 이름 .exception()

await task
Task가 완료될 때까지 대기합니다. Task의 결과를 반환하거나 예외를 발생시킵니다.

task.result()
Task의 결과를 반환하는 메서드입니다. Task가 완료되지 않았으면 예외가 발생합니다.

task.cancel()
Task를 취소하는 메서드입니다. 취소 요청을 보내지만 즉시 취소되지 않을 수 있습니다. 

동기 - 비동기 변환

sync_to_async
Djago의 동기 함수를 비동기 함수로 변환하는 유틸리티입니다. 동기 ORM 쿼리등을 비동기 컨텍스트에서 사용할 수 있게 해줍니다.

SynchrounousOnlyOperation

Djago에서 동기 전용 작업을 비동기 컨텍스트에서 실행하려고 할 때 발생하는 예외입니다.

서브프로세스 관리

asyncio.create_subprocess_exec() 입니다.

pid
process id 입니다. 실행 중인 프로세스를 고유하게 식별하는 번호입니다.

returncode
프로세스 종료 코드입니다. 0은 성공, 0이 아니면 오류를 의미합니다.

wait()
프로세스가 종료될 때까지 대기하는 메서드입니다.

terminate()
프로세스에 종료 신호(SIGTERM)을 보내는 메서드입니다. 정상 종료를 요청합니다.

kill()
프로세스에 강제 종료 신호 (SIGKILL)을 보내는 메서드입니다. 즉시 종료됩니다.

버퍼관리

drain()
버퍼에 남아 있는 데이터를 모두 전송하도록 하는 명령어입니다. 비동기 I/O에서 버퍼를 비우는데 사용됩니다.

버퍼 ㄹ플래시
버퍼에 쌓은 데이터를 싲레로 전송하는 작업

datetime.now()
timezone.now()
Djago에서 현재 UTC 시간을 반환하는 함수입니다. 타임존 인식 datatime 객체를 반환합니다.

타임 아웃
작업이 지정된 시간내에 완료되지 않으면 취소하거나 예외를 발생시키는 메커니즘

Javascript 관련 (참고)

SupplyAsync

CompatableFutrue.suupplyAsync

fetch
thenapply

parseJson

JSON 문자열을 파싱하는 함수.




"""